/**
 * @deprecated Import from gamificationService.js — kept for existing imports.
 */
export {
  logUserActivity,
  checkInStreak,
  syncBadges,
  evaluateRewardUnlocks,
  buildRewardsStatus,
  getRewardsSummary,
  claimGamificationReward,
  BADGE_DEFS,
} from "./gamificationService.js";
